import { SquareColorDirective } from './square-color.directive';

describe('SquareColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SquareColorDirective();
    expect(directive).toBeTruthy();
  });
});
